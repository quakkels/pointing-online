@model List<PointingPoker.DataAccess.Models.User>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>User Name</th>
            <th>Email</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach(var user in Model)
    {
        <tr>
            <td>@user.Id</td>
            <td>@user.UserName</td>
            <td>@user.Email</td>
            <td><a href="@Url.Action("Profile", new { username = user.UserName })">Profile</a></td>
        </tr>
    }
    </tbody>
</table>